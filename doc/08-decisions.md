# 08 — Decisions

Locked decisions are immutable in Phase 1 unless an open question reveals a flaw.
Open questions need an answer before the milestone that depends on them.

## Locked

| # | Decision | Rationale |
|---|---|---|
| **D1** | Plans are `Stream<Item=Msg>` produced by `async-stream::stream!` macro. | Closest visual analog to bluesky `yield Msg` while staying first-class async in Rust. |
| **D2** | `Msg` is a closed `enum` with 26 variants + `Custom { name, payload }` escape hatch. | Compile-time exhaustiveness; bluesky's open registry is rare in practice. |
| **D3** | `Status` implements `Future` AND has ophyd-style sync methods (`add_callback`, `wait`, `done`, `success`). | Dual API requirement; matches both ophyd and ophyd-async users without forking the type. |
| **D4** | Document fan-out via `tokio::sync::broadcast::Sender<Document>`; lagged drops counted in atomic. | K6 enforced at design time. |
| **D5** | Bulk-data path uses `bytes::Bytes` for zero-copy fan-out + downstream-allocator trait. | rogue Pool semantics; cheap clones; works for both PVA monitor and rogue DMA. |
| **D6** | tokio multi-thread runtime is required; current-thread is rejected at startup. | kodex precedent: `block_in_place` panics on current-thread runtimes. |
| **D7** | The `cirrus-py` PyO3 adapter is M7 (deferred); core is Rust-first. | Q1 answer: Rust-first user population. |
| **D8** | Phase 1 DET data path is PVA monitor (areaDetector). rogue is Phase 2. | Q2 answer: typical beamline data rate is ~few hundred MB/s, well within PVA's range; rogue is reserved for direct-attached hardware that genuinely cannot be reached over EPICS. |
| **D9** | Backends bind directly to `epics-rs/crates/epics-ca-rs` and `epics-rs/crates/epics-pva-rs`. | No Python shim; no C library FFI; lives in the same Rust workspace as the user's IOC stack. |
| **D10** | Subscriptions return `SubToken` (RAII). Drop removes the backend slot. | K2 enforced at design time. |
| **D11** | Channel registries use sharded maps + `pending: Notify` for in-flight dedup. | K3 + K4 enforced at design time. |
| **D12** | Document types are auto-generated by `typify` from event-model JSON schemas; no hand-written types. | Cross-language schema parity; zero manual maintenance. |
| **D13** | Three sealed traits: `SignalBackend`, `FrameSource` / `FrameSink`, `DetectorWriter`. Breaking change forbidden after Phase 1. | These are the trait points Phase 2 (rogue) plugs into. |
| **D14** | Two co-equal user-facing modules: `cirrus::ophyd_async` (async, primary) and `cirrus::ophyd` (sync, blanket impl over async via `cirrus_runtime().block_on`). | Equal-status API surfaces requested by the user; sync is not a second-class facade. |
| **D15** | RunEngine has two entry points: `run_async(plan).await` and `run_blocking(plan)`. The latter must not be called from inside an async task. | Mirrors bluesky `RE.__call__` blocking semantics; same plan code drives both. |
| **D16** | The cirrus runtime is a process-singleton `tokio::Runtime` started lazily by `cirrus_core::runtime()`. | Required for the sync facade to call `block_on` from any thread. |

## Project naming

| # | Decision | Rationale |
|---|---|---|
| **N1** | Project name: `cirrus`. | sky/cloud family alignment with bluesky/nimbus/databroker; searchable; conveys lightness. |
| **N2** | Crate prefix: `cirrus-*`. | Distinct from the local `*-rs` suffix convention to mark cirrus as a library, not a port. |
| **N3** | User-facing module names: `cirrus::ophyd_async`, `cirrus::ophyd`, `cirrus::prelude`. | Zero learning cost for migrating users — reuse the names they already know. |

## Open

| # | Question | Default |
|---|---|---|
| **O1** | Do we generate the device tree statically via `#[derive(Device)]` only, or also support a dynamic builder for runtime-configurable composition (read from YAML)? | Static-only for M0–M3; revisit at M6 when CLI users need runtime config. |
| **O2** | Should `cirrus-cli` use `rustyline` (line editor) or `reedline` (Nushell's editor)? | reedline — better hint/history support, async-friendly. |
| **O3** | Do we ship a `cirrus-tiled` callback in M3 or wait? | M3 — Tiled is the canonical bluesky storage; lacking it makes M3 incomplete in practice. |
| **O4** | Should `Wait` group bookkeeping use `JoinSet` or a hand-rolled `HashMap<GroupId, Vec<Status>>`? | `JoinSet` — gets cancellation semantics for free. |
| **O5** | Do we support nested runs (ophyd-async multi-run plans)? | Defer to M4. The pattern is rare and complicates the bundler. |
| **O6** | Should `cirrus-py` expose `RunEngine` as a Python-callable, or only the device classes? | Both — match bluesky's `RE(plan)` call pattern. M7. |
| **O7** | Cargo features for opt-in backends (e.g. `default-features = ["epics-ca", "epics-pva"]`)? | Yes — soft and mock are always on; epics backends gated for users who do not want EPICS as a dep. |

## Rejected

| # | Considered | Why rejected |
|---|---|---|
| `rdaq` (project name) | Earlier candidate. | Does not say which DAQ; breaks the local `*-rs` convention; unsearchable. |
| Backend via `pyepics` / `caproto` shim | Cross-language door from Python ophyd. | Defeats the purpose of a Rust core. |
| C++ FFI to `pvxs` | Reuses libpvxs work. | The user has `epics-pva-rs` already; no reason for FFI. |
| Open string command + dictionary registry (bluesky style) | Maximum extensibility. | Loses compile-time exhaustiveness; the closed enum + `Custom` arm covers the same use cases. |
| Two parallel trait families with separate impls (true peer surfaces) | Maximum independence between sync and async. | Doubles every device impl; maintenance bomb. Blanket impl is the right factoring. |
| `Frame` carrying `Vec<Buffer>` (rogue's literal layout) | Direct rogue port. | `Bytes` already supports chunk chaining; one fewer concept. |
